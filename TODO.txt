- fix client crashing when navigating away from map screen while the map is in mid-animation.
- add dark mode setting.
- support displaying current events.
- decide whether Event.coordinates may be optional.
- implement ui for searching/filtering and sorting events by search keywords, category, follows, date, location, distance from user... etc.
- relationship with friends rules:
    - friend request.
    - event visibility/privacy options (public, friends, family... etc).
- delete expired refresh tokens.
- implement authorization (permissions, access control...).

done:
- implement editing and saving profile pictures.
- relationship with friends rules: follows.
- handle refresh token expiration.
- implement logout in backend.
- fix login.
- search about location data and maps.
- onboarding (UI, viewModel)
- signUP (UI, viewModel, repository)
- login (UI, viewModel, repository)